#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int s, top = -1;
void push(int *stack, int value) {
    if (top == s - 1) {
        printf("Overflow\n");
    } 
    else {
        stack[++top] = value;
    }
}
void pop(int *stack) {
    if (top == -1) {
        printf("Underflow\n");
    } 
    else {
        printf("Popped: %d\n", stack[top]);
        top--;
    }
}
void display(int *stack) {
    if (top == -1) {
        printf("Stack is Empty\n");
    } 
    else {
        for (int i = top; i >= 0; i--) {
            printf("%d ", stack[i]);
        }
        printf("\n");
    }
}
int main() {
    int n, value;
    scanf("%d", &s);
    int stack[s];
    scanf("%d", &n); 
    for (int i = 0; i < n; i++) {
        scanf("%d", &value);
        push(stack, value);
    }
    for (int i = 0; i < 2; i++) {
        pop(stack);
    }
    for (int i = 0; i < 4; i++) {
        scanf("%d", &value);
        push(stack, value);
    }
    pop(stack);
    for (int i = 0; i < 2; i++) {
        scanf("%d", &value);
        push(stack, value);
    }
    for (int i = 0; i < 3; i++) {
        pop(stack);
    }
    display(stack);
    return 0;
}
