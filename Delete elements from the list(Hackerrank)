#include <stdio.h>
int main(){
    int n,pos;
    scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;i++){
        scanf("%d",&list[i]);
    }
    scanf("%d",&pos);
    for(int i=0;i<n;i++){
        if(list[i]==pos){
            for(int j=i;j<n;j++){
                list[j]=list[j+1];
                
            }
            n--;
            break ;
        }
    }
    for(int i=0;i<n;i++){
        printf("%d ",list[i]);
    }
    return 0;
}
