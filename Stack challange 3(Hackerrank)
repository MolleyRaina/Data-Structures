#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int top=-1;
void push(int *stack,int value){
    stack[++top]=value;
}
void display(int *stack){
    for(int i=top;i>=0;i--){
        printf("%d ",stack[i]);
    }
}
int main(){
    int n,value;
    scanf("%d ",&n);
    int stack[n];
    for(int i=0;i<n;i++){
        scanf("%d ",&value);
        push(stack,value);
    }
    for(int i=0;i<n-1;i++){
        for(int j=0;j<n-1-i;j++){
            if(stack[j]>stack[j+1]){
                int temp=stack[j];
                stack[j]=stack[j+1];
                stack[j+1]=temp;
            }
        }
    }
    display(stack);
    return 0;
}
